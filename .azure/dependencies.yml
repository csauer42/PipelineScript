# this doesn't work.  parameters support objects, variables don't.
# eventually this is supposed to change, according to MS.

variables:
- name: BuildDependencies
  type: object
  default:
  - pkg: sample.bin
    version: 1.0.0
  - pkg: main.hex
    version: 0.0.6
  - pkg: avr-common
    version: 2.0.0-1-g0e5955b